    <!-- Testimonial Section -->
    <div class="page-width-desktop slider-cont">
      <div class="testimonial__heading" style="text-transform:uppercase;text-align:center;margin-bottom:30px">
          <!-- <span>Our</span>
          <h2 style="margin:0px;font-size:34px">Community</h2> -->
      </div>
      <div class="testimonial-slider">
        

        {% for block in section.blocks %}
          <div class="testimonial-item">
              <p style="font-size:20px;font-weight: bold !important;
    line-height: 23.4px;">{{ block.settings.name  }}</p>
              <img loading="lazy"  src="{{ block.settings.img | img_url:"master" }}">
               <p style="font-size:16px;font-weight: bold !important;
    line-height: 26px;">{{ block.settings.title  }}</p>
               <p style="font-size:16px;
    line-height: 23.4px;">{{ block.settings.description  }}</p>
          </div>
      {% endfor %}
       
    </div>
    </div>

  <style>
    .slider-cont{
      /* background:#F7F9FB; */
      padding-top:80px;
      padding-bottom:50px
      
    }
    .testimonial-item{
      background:white;
      margin:0px 10px;
      padding:20px
    }
    .testimonial-slider  p{
      margin:10px 0px
    }
    .testimonial-item {
    height: 280px;
}
  </style>
  <script>
       
        $(document).ready(function(){
            $('.testimonial-slider').slick({
                dots: false,
              slidesToShow: 3,
              responsive: [
                    {
                        breakpoint: 768, // Breakpoint for mobile devices
                        settings: {
                            slidesToShow: 1, // Display 1 slide at a time on mobile
                        }
                    }
                   
                ]
            });
        });
    </script>
{% schema %}
  {
    "name": "Testimonial",
    "settings": [],
    "blocks":[
      {
        "name":"Testimonial",
        "type":"Testimonial",
        "settings":[
          {
            "type":"text",
            "id":"name",
            "label":"Name"
          },
          {
            "type":"image_picker",
            "id":"img",
            "label":"Image"
          },
          {
            "type":"text",
            "id":"title",
            "label":"Title"
          },
          {
            "type":"richtext",
            "id":"description",
            "label":"Description"
          }
        ]
      }
    ],
    "presets":[
      {
        "name":"Testimonial"
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}